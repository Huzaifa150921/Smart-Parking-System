import 'package:flutter/material.dart';

class OwnerRegistration extends StatelessWidget {
  const OwnerRegistration({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
